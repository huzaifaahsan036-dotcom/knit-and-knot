import ProductCard from '@/components/ProductCard';import {getCategories,getProducts} from '@/lib/data';import ShopControls from '@/components/ShopControls';
export default async function Shop({searchParams}:{searchParams:Promise<{q?:string;category?:string;sort?:string}>}){const sp=await searchParams;const [{data,error},cats]=await Promise.all([getProducts(sp),getCategories()]);return <div className="container py-14"><div className="mb-10"><p className="text-brand uppercase tracking-[.2em] text-xs font-bold">The collection</p><h1 className="font-display text-5xl mt-2">Handmade, your way.</h1><p className="text-gray-500 mt-3">{sp.q?`Results for “${sp.q}”`:'Accessories crafted for everyday moments and thoughtful gifting.'}</p></div><ShopControls categories={cats} q={sp.q||''} category={sp.category||''} sort={sp.sort||''}/>{error?<div className="p-10 bg-white rounded-3xl">Could not load products right now. Please try again.</div>:data.length?<div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 md:gap-6 mt-8">{data.map(p=><ProductCard key={p.id} p={p}/>)}</div>:<div className="py-20 text-center bg-white rounded-[30px] mt-8"><h2 className="font-display text-3xl">Nothing matched that search.</h2><p className="text-gray-500 mt-2">Try another keyword or browse all handmade pieces.</p></div>}</div>}
