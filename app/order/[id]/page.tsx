'use client';import {useEffect,useState} from 'react';import Image from 'next/image';import Link from 'next/link';import {supabase} from '@/lib/supabase';export default function Order({params}:{params:Promise<{id:string}>}){const[id,setId]=useState('');const[data,setData]=useState<any>(null);const[loading,setLoading]=useState(true);useEffect(()=>{params.then(p=>setId(p.id))},[params]);useEffect(()=>{if(!id)return;const token=new URLSearchParams(window.location.search).get('token');supabase.rpc('get_public_order',{p_order_id:id,p_lookup_token:token}).then(({data})=>{setData(data);setLoading(false)})},[id,sp]);if(loading)return <div className="container py-24 text-center">Loading your order…</div>;if(!data?.order)return <div className="container py-24 text-center"><h1 className="font-display text-4xl">Order not found</h1><p className="text-gray-500 mt-2">This confirmation link may be incomplete.</p><Link href="/shop" className="inline-block mt-6 bg-ink text-white rounded-full px-6 py-3">Back to shop</Link></div>;const o=data.order;return <div className="container py-14 max-w-3xl"><div className="text-center"><Image src="/logo.jpg" width={130} height={90} alt="Knit & Knot" className="mx-auto rounded-xl"/><p className="text-brand uppercase tracking-[.2em] text-xs font-bold mt-7">Order confirmed</p><h1 className="font-display text-5xl mt-2">Thank you for your order!</h1><p className="text-gray-500 mt-3">Order #{o.id.slice(0,8).toUpperCase()}</p></div><div className="bg-white border rounded-3xl p-7 mt-10"><h2 className="font-semibold">Items</h2>{data.items.map((x:any,i:number)=><div key={i} className="flex justify-between py-4 border-b text-sm"><span>{x.product_name} × {x.quantity}</span><span>Rs. {(x.unit_price*x.quantity).toLocaleString()}</span></div>)}<div className="flex justify-between mt-5"><span>Delivery</span><span>Rs. {Number(o.shipping).toLocaleString()}</span></div><div className="flex justify-between mt-2 text-xl"><b>Total</b><b>Rs. {Number(o.total).toLocaleString()}</b></div></div><div className="grid md:grid-cols-2 gap-4 mt-5"><div className="bg-white border rounded-3xl p-6"><b>Payment</b><p className="text-gray-500 mt-2">Cash on Delivery</p><p className="text-xs text-brand mt-1">Payment pending until delivery.</p></div><div className="bg-white border rounded-3xl p-6"><b>Delivery to</b><p className="text-gray-500 mt-2">{o.shipping_name}<br/>{o.shipping_address}<br/>{o.shipping_city}</p></div></div><Link href="/shop" className="block text-center mt-8 bg-ink text-white rounded-full py-4">Continue shopping</Link></div>}
