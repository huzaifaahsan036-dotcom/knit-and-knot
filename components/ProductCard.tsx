'use client';import Image from 'next/image';import Link from 'next/link';import {ShoppingBag} from 'lucide-react';import {useCart} from './Providers';import type {Product} from '@/types';
export default function ProductCard({p}:{p:Product}){const cart=useCart();const src=p.image_url||'/logo.jpg';const price=p.sale_price??p.price;return <article className="group [perspective:1000px]"><div className="bg-white rounded-[28px] overflow-hidden border border-pink-100 shadow-soft transition duration-500 group-hover:-translate-y-2 group-hover:[transform:rotateX(1deg)_rotateY(-2deg)]"><Link href={'/product/'+p.slug} className="block relative aspect-square overflow-hidden bg-blush/30"><Image src={src} alt={p.name} fill sizes="(max-width:768px) 50vw, 280px" className="object-cover transition duration-700 group-hover:scale-105"/><span className="absolute left-4 top-4 rounded-full bg-white/90 px-3 py-1 text-[11px] font-semibold uppercase tracking-widest">{p.categories?.name||'Handmade'}</span></Link><div className="p-5"><Link href={'/product/'+p.slug}><h3 className="font-semibold text-lg">{p.name}</h3></Link><div className="flex items-center justify-between mt-3"><div><span className="font-bold">Rs. {price.toLocaleString()}</span>{p.sale_price&&<span className="ml-2 text-xs text-gray-400 line-through">Rs. {p.price.toLocaleString()}</span>}</div><button disabled={p.stock<1} onClick={()=>cart.add(p)} className="rounded-full p-3 bg-ink text-white disabled:opacity-40 hover:bg-brand" aria-label="Add to cart"><ShoppingBag size={17}/></button></div></div></div></article>}
