'use client';import {useState} from 'react';export default function Newsletter(){const[email,setEmail]=useState('');const[msg,setMsg]=useState('');async function go(e:any){e.preventDefault();const r=await fetch('/api/newsletter',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});setMsg(r.ok?'You’re on the list.':'Please check your email and try again.');if(r.ok)setEmail('')}return <form onSubmit={go} className="flex flex-col sm:flex-row gap-2"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Your email address" className="flex-1 rounded-full px-5 py-3 bg-white outline-none"/><button className="rounded-full bg-ink text-white px-6 py-3 font-semibold">Join</button>{msg&&<span className="text-xs text-gray-500 self-center">{msg}</span>}</form>}
