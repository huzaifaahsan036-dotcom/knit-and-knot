'use client';import {useState} from 'react';import {supabase} from '@/lib/supabase';export default function AdminUploader({bucket,pathPrefix,onUploaded}:{bucket:string;pathPrefix:string;onUploaded:(path:string)=>void}){const[busy,setBusy]=useState(false);const[msg,setMsg]=useState('');async function upload(file:File){if(!['image/jpeg','image/png','image/webp'].includes(file.type)||file.size>5*1024*1024){setMsg('Use JPG, PNG or WebP under 5MB.');return}setBusy(true);setMsg('Uploading…');const ext=file.name.split('.').pop();const path=`${pathPrefix}/${crypto.randomUUID()}.${ext}`;const{error}=await supabase.storage.from(bucket).upload(path,file,{upsert:false,contentType:file.type});if(error)setMsg(error.message);else{setMsg('Uploaded');onUploaded(path)}setBusy(false)}return <label className="inline-flex items-center gap-2 rounded-full border px-4 py-2 text-xs cursor-pointer">{busy?'Uploading…':'Upload image'}<input type="file" accept="image/jpeg,image/png,image/webp" className="hidden" onChange={e=>{const f=e.target.files?.[0];if(f)upload(f)}}/></label>}
