'use client';import {useRouter} from 'next/navigation';import {Search,SlidersHorizontal} from 'lucide-react';import {useState} from 'react';export default function ShopControls({categories,q,category,sort}:{categories:any[];q:string;category:string;sort:string}){const r=useRouter();const[s,setS]=useState(q);function go(next:any){const p=new URLSearchParams();Object.entries({...{q:s},...next}).forEach(([k,v])=>{if(v)p.set(k,String(v))});r.push('/shop?'+p.toString())}return <div className="bg-white rounded-3xl border border-pink-100 p-4 flex flex-col md:flex-row gap-3"><div className="flex-1 flex items-center bg-cream rounded-2xl px-4"><Search size={18}/><input value={s} onChange={e=>setS(e.target.value)} onKeyDown={e=>e.key==='Enter'&&go({})} placeholder="Search products, categories, tags…" className="bg-transparent outline-none p-3 w-full"/></div><select aria-label="Category" value={category} onChange={e=>go({category:e.target.value})} className="bg-cream rounded-2xl px-4 py-3 outline-none"><option value="">All categories</option>{categories.map(c=><option key={c.id} value={c.slug}>{c.name}</option>)}</select><select aria-label="Sort" value={sort} onChange={e=>go({sort:e.target.value})} className="bg-cream rounded-2xl px-4 py-3 outline-none"><option value="">Featured & newest</option><option value="price-asc">Price: low to high</option><option value="price-desc">Price: high to low</option></select><div className="hidden md:grid place-items-center px-2 text-gray-400"><SlidersHorizontal size={18}/></div></div>}
